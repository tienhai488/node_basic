<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      .container {
        text-align: center;
      }
      .container-form {
        margin-top: 10px;
        margin-bottom: 15px;
      }
      .container-form label {
        width: 60px;
        display: inline-block;
      }
      .container button {
        cursor: pointer;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <form action="/submit-edit-user" method="POST">
        <h1>Edit new User</h1>
        <input type="text" name="id" hidden value="<%= userEdit.id %>" />
        <div class="container-form">
          <label for="">Name</label>
          <input type="text" name="name" value="<%= userEdit.name %>" />
        </div>
        <div class="container-form">
          <label for="">Email</label>
          <input type="email" name="email" value="<%= userEdit.email %>" />
        </div>
        <div class="container-form">
          <label for="">Phone</label>
          <input type="text" name="phone" value="<%= userEdit.address %>" />
        </div>
        <div class="container-form">
          <label for="">Address</label>
          <input type="text" name="address" value="<%= userEdit.phone %>" />
        </div>
        <button type="submit">SUBMIT</button>
      </form>
    </div>
  </body>
</html>
